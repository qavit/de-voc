# 原始資料與匯出庫 (Data)

這裡存放使用者的實體單字原始母檔，例如最一開始使用的 `.xlsx` 檔案，以及未來從此系統匯出的歷史 JSON 備份或預處理檔案（如 `shift_review.csv`）。

> **注意：** 基於保護使用者個人學習進度並縮減儲存庫體積的原則，本目錄下的所有資料（除了這份 README 檔以外）都在 Git 版本控制忽略清單中。如果想要備份原始資料庫內容，請利用系統匯出功能，切勿直接 push 母檔。
